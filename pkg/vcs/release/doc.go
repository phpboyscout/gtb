// Package release provides the shared release model ([Release], [ReleaseAsset])
// and a [Provider] factory that resolves the correct VCS backend (GitHub or
// GitLab) from tool configuration for use by the self-update system.
package release
