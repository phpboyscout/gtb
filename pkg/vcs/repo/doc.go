// Package repo provides repository URL parsing and metadata extraction,
// converting host/owner/repo paths into a [RepoLike] abstraction that supports
// both GitHub (org/repo) and GitLab (group/subgroup/repo) path formats.
package repo
