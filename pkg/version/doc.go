// Package version provides semantic version parsing, comparison, and
// development-build detection via the [Version] interface. Build-time
// version strings injected via ldflags are parsed into structured values
// for display and update-check comparison.
package version
