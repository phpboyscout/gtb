// Package templates provides Go template definitions and data structures used by
// the generator to produce CLI command scaffolding, registration code (cmd.go),
// and implementation stubs (main.go). Template data types like [CommandData] carry
// the resolved configuration for each generated command.
package templates
