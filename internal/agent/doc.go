// Package agent provides tool definitions for the autonomous agentic verification
// loop used during code generation. Tools include sandboxed file I/O (read, write,
// list), Go toolchain operations (build, test, get, mod tidy), and linting — all
// scoped to a project root directory for safety.
package agent
